%nav
  %section.nav_left
    %h3
      = link_to('Pad Crash', root_path)
  %section.nav_right
    %ul
      - if current_user
        = render(:partial => "layouts/user_nav")
      - else
        = render(:partial => "layouts/guest_nav")
